---
import BaseLayout from "../layouts/BaseLayout.astro";
import TimelineItem from "../components/TimelineItem.astro";
import SectionHeading from "../components/SectionHeading.astro";
import { site } from "../data/site";
import { timeline } from "../data/timeline";
import { withBase } from "../utils/url";

const scienceSkills = ["Neuroscience", "Research methods", "Data analysis", "Scientific writing"];
---

<BaseLayout
  title={`About · ${site.name}`}
  description="Neuroscience grad working toward medical school."
>
  <div class="mx-auto max-w-3xl px-6 py-16">
    <h1 class="text-2xl font-semibold tracking-tight text-text">About</h1>

    <div class="prose mt-6">
      <p>
        Hi, I'm Rohit. I studied neuroscience because I wanted to understand the organ that
        makes us who we are — and the more I learned, the more certain I became that I want to
        spend my career in medicine. Right now, my focus is earning a seat in medical school.
      </p>
      <p>
        Along the way I picked up a research habit: I spent time in an undergraduate research
        program analyzing data, learned how to read papers properly, and found out that the
        questions get better the deeper you go. I think that curiosity is the best thing a
        future physician can carry into the room.
      </p>
      <p>
        This site is my home on the internet — a record of the journey and the occasional
        thing I can't stop thinking about.
      </p>
    </div>

    <section class="mt-16">
      <SectionHeading title="Journey" />
      <div class="mt-7 flex flex-col">
        {timeline.map((entry) => <TimelineItem entry={entry} />)}
      </div>
    </section>

    <section class="mt-16">
      <SectionHeading title="Toolbox" />
      <div class="mt-5 flex flex-wrap gap-2">
        {
          scienceSkills.map((s) => (
            <span class="rounded-full border border-border px-2.5 py-0.5 text-xs text-text-muted">
              {s}
            </span>
          ))
        }
      </div>
    </section>

    <section class="mt-16">
      <SectionHeading title="Beyond the desk" />
      <p class="mt-4 max-w-2xl text-sm leading-relaxed text-text-muted">
        Outside of studying, I'm usually reading, at the gym, or going down a Wikipedia rabbit
        hole.
      </p>
    </section>

    <section class="mt-16 border-t border-border pt-10">
      <p class="text-text-muted">
        Want to follow along? Start with <a
          href={withBase("/blog")}
          class="text-accent underline hover:text-accent-hover"
        >the blog</a>.
      </p>
    </section>
  </div>
</BaseLayout>
